# 🏋️‍♂️ Homegym Online Ordering System

โปรเจกต์ระบบสั่งซื้อสินค้าออนไลน์แบบ Full-stack สำหรับร้านอุปกรณ์ออกกำลังกาย (Homegym) โดยใช้ Node.js, Express, และ MySQL

## 📂 โครงสร้างโปรเจกต์ (Project Structure)

โปรเจกต์นี้มีโครงสร้างที่น่าสนใจโดยมีการแยกส่วน Backend และ Frontend อย่างชัดเจน:

```
.
├── server.js                # เซิร์ฟเวอร์หลัก (Node.js + Express)
├── package.json             # รายการ Dependencies และ Scripts
├── config.env               # ไฟล์ตั้งค่า Environment Variables (DB Host, User, Pass)
├── database.sql             # สคริปต์สร้างฐานข้อมูล MySQL พร้อมข้อมูลตัวอย่าง (แนะนำ)
├── routes/                  # API Routes แยกตามโมดูล
│   ├── products.js          # จัดการรายการสินค้า
│   ├── orders.js            # จัดการคำสั่งซื้อ
│   ├── customers.js         # จัดการข้อมูลลูกค้า
│   └── promotions.js        # จัดการโปรโมชั่น
├── node_modules/config/     # (Non-standard) ที่เก็บไฟล์ database.js สำหรับเชื่อมต่อ DB
├── images/                  # รูปภาพประกอบสินค้าและเว็บไซต์
├── videos/                  # วิดีโอโปรโมท (v1.mp4)
├── index.html               # หน้าแรกของเว็บไซต์
├── products.html            # หน้ารายการสินค้าทั้งหมด
├── contact.html             # หน้าติดต่อเรา
├── order-details.html       # หน้าแสดงรายละเอียดคำสั่งซื้อ
├── script.js                # Logic หลักสำหรับหน้าแรก (Carousel, Filtering)
├── products.js              # Logic ระบบตะกร้าสินค้าและการจัดการ Product Modal
└── styles.css, products.css # ไฟล์สไตล์ CSS
```

## 🛠️ เทคโนโลยีที่ใช้ (Tech Stack)

### Backend:
- **Runtime:** Node.js
- **Framework:** Express.js
- **Database:** MySQL (เชื่อมต่อผ่าน `mysql2` พร้อมระบบ Connection Pool)
- **Middleware:** `cors`, `body-parser`, `dotenv`

### Frontend:
- **Languages:** HTML5, CSS3, JavaScript (Vanilla JS)
- **Features:**
  - **Responsive Design:** รองรับมือถือและเดสก์ท็อป
  - **Cart System:** ระบบตะกร้าสินค้าที่บันทึกข้อมูลใน LocalStorage
  - **Modal System:** แสดงรายละเอียดสินค้าแบบเจาะลึก
  - **Carousel:** สไลด์โฆษณาที่รองรับการปัด (Swipe) บนมือถือ
  - **Search & Filter:** ค้นหาและกรองสินค้าตามหมวดหมู่

## 📊 ฐานข้อมูล (Database Schema)

ฐานข้อมูล `homegym` ประกอบด้วยตารางหลักดังนี้:
1.  **categories:** หมวดหมู่สินค้า (Weight Training, Cardio, Yoga, Apparel, etc.)
2.  **products:** รายการสินค้า ราคา รายละเอียด และสถานะยอดฮิต
3.  **customers:** ข้อมูลลูกค้า (ชื่อ, เบอร์โทร, ที่อยู่)
4.  **orders:** ข้อมูลหลักของคำสั่งซื้อ เลขที่ใบสั่งซื้อ และสถานะ (Pending, Delivered, etc.)
5.  **order_items:** รายการสินค้าในแต่ละคำสั่งซื้อ
6.  **promotions:** โค้ดส่วนลดและโปรโมชั่น
7.  **order_audit:** ตารางบันทึกประวัติการเปลี่ยนสถานะคำสั่งซื้อ (Trigger-based)

**ฟีเจอร์ฐานข้อมูลขั้นสูง:**
- **Stored Procedures:** `CreateOrder`, `GetProductsByCategory`, `GetOrderDetails`
- **Views:** `popular_products`, `order_summary`, `product_popularity`
- **Triggers:** ระบบ Audit Log อัตโนมัติเมื่อสถานะออเดอร์เปลี่ยน

## 🚀 วิธีการติดตั้ง (Installation)

1. **ติดตั้ง Dependencies:**
   ```bash
   npm install
   ```

2. **ตั้งค่าฐานข้อมูล:**
   - นำไฟล์ `database.sql` ไปรันใน MySQL Server
   - แก้ไขไฟล์ `config.env` เพื่อระบุรายละเอียดการเชื่อมต่อ:
     ```env
     DB_HOST=localhost
     DB_USER=root
     DB_PASSWORD=รหัสผ่านของคุณ
     DB_NAME=homegym
     DB_PORT=3306
     ```

3. **เริ่มใช้งาน:**
   ```bash
   # สำหรับโหมดพัฒนา (ต้องมี nodemon)
   npm run dev
   
   # สำหรับโหมดปกติ
   npm start
   ```
   เซิร์ฟเวอร์จะทำงานที่: `http://localhost:3000`

## 🔌 API Endpoints ที่สำคัญ

- `GET /api/products`: ดึงรายการสินค้าทั้งหมด
- `GET /api/products/popular`: ดึงรายการสินค้ายอดฮิต
- `POST /api/orders`: บันทึกคำสั่งซื้อใหม่
- `GET /api/health`: ตรวจสอบสถานะการเชื่อมต่อเซิร์ฟเวอร์และฐานข้อมูล

## 💡 ข้อมูลเพิ่มเติม

- **ระบบตะกร้าสินค้า:** ใช้ JavaScript จัดการสถานะและจัดเก็บใน `localStorage` เพื่อให้ข้อมูลไม่หายเมื่อ Refresh หน้าเว็บ
- **ระบบสถิติ:** ในไฟล์ `products.js` มีระบบเก็บสถิติการคลิกและการสั่งซื้อสินค้าแยกตาม ID

---
*จัดทำโดย: ทีมพัฒนา Homegym*